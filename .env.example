DJANGO_SECRET_KEY=your-secret-key-here
DJANGO_DEBUG=True
DJANGO_SETTINGS_MODULE=config.settings.dev
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
DATABASE_URL=postgres://postgres:postgres@localhost:5432/booking_db
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# Email (console backend in dev, SMTP in prod)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_HOST_USER=apikey
# EMAIL_HOST_PASSWORD=your-sendgrid-api-key
# EMAIL_USE_TLS=True
# DEFAULT_FROM_EMAIL=noreply@bloomflow.app

# AI Copilot (optional — copilot endpoint returns a fallback message if unset)
# Get a key at https://aistudio.google.com/apikey
# GEMINI_API_KEY=your-gemini-api-key-here
